services:
  assistant_db:
    container_name: assistant_db
    image: postgres:17.5
    env_file: "../.env"
    ports:
      - "5432:5432"
    volumes:
      - assistant_pgdata:/var/lib/postgres/assistant

volumes:
  assistant_pgdata: